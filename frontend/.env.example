# Backend API URL
VITE_API_URL=http://localhost:8000

# Auth0 Configuration (for production)
# These are configured in the backend for Auth0 authentication
# No frontend configuration needed for Auth0 - it's handled server-side
